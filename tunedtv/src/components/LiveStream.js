// components/LiveStream.js
import React from 'react';
import './App.css';

function LiveStream() {
  return (
    <div className="live-stream">
      <h1>Live Stream</h1>
      <p>Tune in to our live sim racing broadcasts here:</p>
      {/* Add live stream player or embed code */}
    </div>
  );
}

export default LiveStream;